name: test-gcp-credentials

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions-hub/gcloud@master
      env:
        PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        APPLICATION_CREDENTIALS: ${{ secrets.GC_SERVICE_ACCT_KEY }}
      with:
        args: info
